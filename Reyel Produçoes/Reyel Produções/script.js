/* Reyel */

function openModal(reyel) {
    document.getElementById(reyel).style.display = "block";
}

function closeModal(reyel) {
    document.getElementById(reyel).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Gilton 

function openModal(gilton) {
    document.getElementById(gilton).style.display = "block";
}

function closeModal(gilton) {
    document.getElementById(gilton).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Yasmym 

function openModal(yasmym) {
    document.getElementById(yasmym).style.display = "block";
}

function closeModal(yasmym) {
    document.getElementById(yasmym).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Eduardo 

function openModal(eduardo) {
    document.getElementById(eduardo).style.display = "block";
}

function closeModal(eduardo) {
    document.getElementById(eduardo).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Sofia 

function openModal(sofia) {
    document.getElementById(sofia).style.display = "block";
}

function closeModal(sofia) {
    document.getElementById(sofia).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Degman 

function openModal(degman) {
    document.getElementById(degman).style.display = "block";
}

function closeModal(dean) {
    document.getElementById(dean).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Arthur 

function openModal(arthur) {
    document.getElementById(arthur).style.display = "block";
}

function closeModal(arthur) {
    document.getElementById(arthur).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Danilo 

function openModal(danilo) {
    document.getElementById(danilo).style.display = "block";
}

function closeModal(danilo) {
    document.getElementById(danilo).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Marta 

function openModal(marta) {
    document.getElementById(marta).style.display = "block";
}

function closeModal(marta) {
    document.getElementById(marta).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}

// Dheyson 

function openModal(dheyson) {
    document.getElementById(dheyson).style.display = "block";
}

function closeModal(dheyson) {
    document.getElementById(dheyson).style.display = "none";
}

// Fecha o modal se o usuário clicar fora dele
window.onclick = function(event) {
    var modals = document.getElementsByClassName('modal');
    for (var i = 0; i < modals.length; i++) {
        if (event.target == modals[i]) {
            modals[i].style.display = "none";
        }
    }
}